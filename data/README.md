# Data sources and access

Document data here rather than committing large or restricted datasets.

| Source | Description | Access instructions | Usage restrictions |
| --- | --- | --- | --- |
| To be added | TBD | TBD | TBD |

Keep local input files in `data/raw/` and generated datasets in `data/processed/`. Both paths are ignored by Git; create them locally when needed. Document preprocessing steps and versions so teammates can reproduce experiments.
